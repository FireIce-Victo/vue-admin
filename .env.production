# 生产环境配置
NODE_ENV=production

# 生产服务器端口（通常用于预览）
VITE_APP_PORT=4173

# API基础URL
# VITE_API_BASE_URL=https://api.example.com/api

# WebSocket 握手路径（需与后端 .env 的 WS_PATH 保持一致）
# 生产同源部署：由 nginx 透传 Upgrade 头到后端（见 WebSocket模块设计方案.md §8）
VITE_WS_PATH=/ws
